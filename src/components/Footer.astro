---
import { TITLES } from '../lib/data'

interface Props {
    locale: string
}

const { locale } = Astro.props

const currentYear = new Date().getFullYear()

const titles = locale === 'es' ? TITLES.es : TITLES.en

---

<footer class="opacity-80 m-4 min-[375px]:pl-4 md:pl-0 mt-16 w-full mx-auto container lg:max-w-4xl md:max-w-2xl mb-10 flex justify-center">
  <div class="rounded-lg w-full max-w-screen-xl mx-auto md:flex md:items-center md:justify-between py-4" >
    <span class="text-sm sm:text-center text-zinc-200/90">
        © {currentYear} 
        <a href="https://sergiotr-minimalist.netlify.app/" 
           target="_blank" 
           rel="noopener noreferrer" 
           class="hover:underline">
            sergiotr
        </a>.
        {titles.footer}
    </span>
    <ul class="flex flex-wrap items-center mt-3 text-sm font-medium dark:text-white/90 sm:mt-0" >
      <li>
        <a href="/#about" class="hover:underline me-4 md:me-6">{titles.about}</a>
      </li>
      <li>
        <a id="contacto" href="mailto:truebanosergio@gmail.com" class="hover:underline">{titles.contact}</a>
      </li>
    </ul>
  </div>
</footer>